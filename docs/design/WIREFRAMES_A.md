# DeepCode 低保真线框（方向 A 细化）

> 日期：2026-08-05
> 状态：DRAFT（S3-02 步骤 3；方向 A = 收敛 New 壳 + 常驻 Sidebar）
> 决策：默认采用方向 A（审计证据最支持：Legacy 5 层过载 / New 无导航；A 为 2 层 + 可收 Sidebar）。用户可在 S3-03 门禁前改选。

## F1 欢迎页（empty/offline）

```
┌──────────────────────────────────────┐
│ DeepCode                    [设置] ⚙  │
│                                      │
│        开始使用 DeepCode              │
│   [ 打开项目目录 ]  [ 新建会话 ]      │
│                                      │
│   离线: 显示缓存会话（只读）          │
└──────────────────────────────────────┘
```

## F2 目录索引（loading/error）

```
┌──────────────────────────────────────┐
│ 索引中: src/           [取消]        │
│ ▓▓▓▓▓▓░░░░ 60%                       │
│  → 失败: 目录不可读                  │
│   [重试] [选择其他目录]               │
└──────────────────────────────────────┘
```

## F3 Provider 配置（permission）

```
┌──────────────────────────────────────┐
│ 需要配置模型 Provider                 │
│ 使用 DeepCode 需要 API Key            │
│ [provider] [api key ████████]        │
│ [保存]（密钥只存本地 .env）          │
└──────────────────────────────────────┘
```

## F4 会话主视图（running/cancel）

```
┌──────────┬─────────────────────────────┐
│ Sidebar  │ 消息流                      │
│ ● Home   │ [用户] 实现登录功能          │
│ ▸ 会话1  │ [Agent] 计划:               │
│   会话2  │  1. 添加 auth 路由 ▸         │
│   归档   │  2. 实现 session 中间件     │
│ ───────  │ ┌ 工具: edit auth.ts ─────┐ │
│ 目录     │ │ Step 1/2 执行中          │ │
│          │ └ [取消] ─────────────────┘ │
│          │ [输入…] [发送]              │
├──────────┴─────────────────────────────┤
│ 队列 0 · 在线                          │
└────────────────────────────────────────┘
```

## F5 失败/恢复（error/recovery）

```
┌──────────────────────────────────────┐
│ 步骤 2 失败: Provider 限流            │
│ ✗ session 中间件未完成                │
│ [重试] [回退到步骤 1] [继续执行]      │
└──────────────────────────────────────┘
```

## F6 权限确认（permission-ask）

```
┌──────────────────────────────────────┐
│ 允许 Agent 执行?                      │
│ 命令: git commit -m "..."            │
│ 工作区: /Users/me/proj               │
│ [拒绝] [允许一次] [始终允许]          │
└──────────────────────────────────────┘
```

## F7 diff/review（empty/loading）

```
┌──────────┬─────────────────────────────┐
│ Sidebar  │ diff/review                 │
│ 会话1    │ 变更: src/auth.ts (+12-3)   │
│ 会话2    │ @@ login() @@               │
│          │ - old impl                  │
│          │ + new impl                  │
│          │ [接受] [拒绝] [行内评论]    │
│          │ loading: diff 生成中 ▓▓░░   │
└──────────┴─────────────────────────────┘
```

## 状态覆盖核对

| 状态 | 线框 | 键盘路径 | 说明 |
|---|---|---|---|
| permission | F3/F6 | Tab 全可达 | F6 危险操作显示对象与可恢复性 |
| loading | F2/F7 | — | 骨架 + 进度 |
| empty | F1/F7 | Enter 打开 | — |
| offline | F1 | — | 只读缓存 |
| error | F2/F5 | Esc 关闭 | 重试/回退 |
| cancel | F4 | Esc | 回执"已取消" |
| recovery | F2/F5 | — | 重试/回退步骤 |

## viewport 验证

- 1024×700：Sidebar 可收（汉堡）；F4 计划面板折叠
- 1440×900：主目标，全组件展开
- 1728×1117：宽消息流 + 可开 Inspector（方向 A 可选扩展）
- 最小支持尺寸草案：1024×700（待原型测试冻结）
