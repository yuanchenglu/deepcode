# DeepCode 实施交接提示词（2026-07-28）

> 用途：在新的 ChatGPT / Codex 会话中直接粘贴本文件的“可复制提示词”，从远程仓库继续实施，不依赖旧容器或旧聊天上下文。
>
> 远程仓库：`https://github.com/yuanchenglu/deepcode.git`

## 当前远程状态

- 开发分支：`develop`
- 发布分支：`master`
- 已合入 `develop`：PR #2，S1-02-C，squash commit `babc3080f9d5c5c90dcf4abb16a0149e6dbc1eb8`
- 当前实施分支：`agent/s1-02-d-install-uninstall-guard`
- 当前 PR：#6 `fix(lifecycle): fail closed DeepCode install and uninstall boundaries`
- PR #6 当前基线：`develop@babc3080f9d5c5c90dcf4abb16a0149e6dbc1eb8`
- 写入本交接文档前的实现 head：`06280508d4004104698987c23505794425a834ef`
- 当前代码、测试和证据均已写入远程分支，不存在只留在旧容器内的必要修改。

## 整体计划进度

- 三阶段总计划：实施中；产品仍为 `NO-GO`。
- 第一阶段：进行中。
  - S1-01A：`DONE`
  - S1-01B：`NOT_STARTED`（官网源码/部署入口和错误安装信息止损）
  - S1-02：`IN_PROGRESS`
    - A：`DONE`
    - B：`DONE`
    - C：`DONE`，已合入 `develop`
    - D：`IN_REVIEW`，PR #6
    - E：`NOT_STARTED`
  - S1-03 及后续第一阶段任务：`NOT_STARTED`
- 第二阶段：全部 `NOT_STARTED`，受 Gate 1 阻塞。
- 第三阶段：全部 `NOT_STARTED`，受 Gate 2 和设计门禁阻塞。

## S1-02-D 已实施内容

- 安装检测只认可 `.deepcode/bin/deepcode` 或 `deepcode.exe`。
- `.opencode/bin/opencode`、`.local/bin/deepcode`、错误二进制名全部 fail-closed 为 `unknown`。
- 删除 OpenCode/anomalyco/npm/brew/choco/scoop 等上游版本查询和升级执行路径。
- S1-03 建立可信 DeepCode Release 渠道前，`latest()` 安全返回当前安装版本。
- 所有 upgrade method 返回类型化禁用错误，不下载、不执行包管理器或安装脚本。
- 卸载只处理 DeepCode 自有目录、二进制和精确 shell PATH 标记。
- 明确保留 `.opencode`、`# opencode` 和 OpenCode PATH 内容。
- 已更新安装测试、新增卸载隔离测试和 S1-02-D evidence。

## 开发流程规范

第一优先：功能分支 → PR → CI → squash 合入 `develop`。

只有 PR/CI 持续异常且完成根因分析、尝试修复后仍无法解决，才允许直推 `develop`。直推 commit 必须使用：

```text
<type>(<scope>): <变更说明>

## 问题原因
<说明 PR/CI 无法通过的根因和已尝试的修复>

## 技术债务
- <遗留事项；无则明确写“无”>
```

DeepCode 技术债务也可记录于 `docs/BUG_LIST.md`，格式：

```text
[日期] 描述 | 遗留原因 | 状态
```

## 可复制到新会话的提示词

```text
@GitHub yuanchenglu/deepcode

请从远程仓库继续实施 DeepCode 三阶段交付计划。不要依赖任何旧容器或未推送本地文件，远程仓库是唯一事实源。

一、先读取并遵守
1. 仓库根 AGENTS.md
2. docs/open-source-readiness/PLAN.md
3. docs/open-source-readiness/HANDOFF_2026-07-28.md
4. docs/open-source-readiness/evidence/S1-02/README.md
5. docs/open-source-readiness/evidence/S1-02/test-results/S1-02-D-lifecycle.md
6. PR #6：fix(lifecycle): fail closed DeepCode install and uninstall boundaries

二、先核验远程事实
- develop 当前 HEAD
- PR #6 当前 head、状态、mergeability、changed files
- typecheck/test workflow 的最终结果和失败日志（如有）
- PR #6 是否已经合入 develop
- agent/s1-02-d-install-uninstall-guard 是否仍存在

已知基线：
- PR #2 / S1-02-C 已 squash 合入 develop
- merge commit：babc3080f9d5c5c90dcf4abb16a0149e6dbc1eb8
- S1-02-D 当前 PR：#6
- 写入交接文档前的实现 head：06280508d4004104698987c23505794425a834ef

三、执行规则
- develop 是开发分支，master 是发布分支。
- 第一优先走：功能分支 → PR → CI → squash 合入 develop。
- 不要因为 develop 可直推就默认直推。
- 只有 PR/CI 持续异常、已完成根因分析并尝试修复仍无法解决时，才直推 develop。
- 直推 commit 必须包含“## 问题原因”和“## 技术债务”。
- 每次只领取一个 PLAN 任务或明确子任务，不跨门禁。
- 不从仓库根目录运行测试；按 PLAN 在对应 package 目录运行。
- 不关闭检查、不删除失败测试、不通过 fallback 恢复 OpenCode 用户边界。

四、当前任务处理
A. 如果 PR #6 的全部 CI 已通过：
1. 核验代码和 evidence 与 PLAN 的 S1-02-D 验收条件一致。
2. 将 S1-02-D 状态和最终 CI/commit/PR 证据更新为 DONE。
3. squash 合入 develop。
4. 从最新 develop 创建独立分支，开始 S1-02-E 全链共存回归和字符串分类。

B. 如果 PR #6 有失败：
1. 获取失败 job 和完整日志/Artifact。
2. 分类为 PRODUCT_DEFECT、TEST_DEFECT、ENV_BLOCKED 或 FLAKY。
3. 做最小根因修复，推回现有 PR 分支。
4. 重新运行完整门禁，不得跳过 Windows/Linux unit、E2E、typecheck、HttpApi。

C. 如果 PR #6 已合入：
1. 从最新 develop 核验 S1-02-D evidence 是否完整。
2. 直接创建 S1-02-E 独立分支并实施。

五、S1-02-E 的目标
- 对 packages/core、packages/opencode 中 opencode/OPENCODE 字符串逐条分类：内部命名、上游协议/Schema/Package 兼容、用户边界遗漏。
- 运行 DeepCode-only、OpenCode-only、双配置并存和生命周期隔离回归。
- 验证 DeepCode 安装、首次运行、升级拒绝、卸载前后 OpenCode 目录树不变。
- 只修复明确遗漏；构建制品、Release installer、checksum、rollback、purge 属于 S1-03，不提前实现。
- 全部子任务和整链回归完成后，才能把 S1-02 父任务标记 DONE。

请自主执行，不要只做状态汇报。完成当前可完成工作后，明确列出：已合入 develop 的内容、当前分支/PR/commit、CI 结果、下一唯一执行点、仍存在的外部阻塞。
```

## 交接边界

- 本文件是远程恢复入口，不替代 `PLAN.md`。
- 新会话必须重新读取 GitHub 当前状态；本文件中的 CI 状态可能在写入后变化。
- 不得依据本文件直接宣称 PR #6 已合并，必须以 GitHub 实时状态为准。
